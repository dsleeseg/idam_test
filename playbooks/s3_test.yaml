---
    - hosts: tag_Name_DAMPOCAuthor
      user: ubuntu

      tasks:
      - name: ensure file exists
        aws_s3: bucket=adobedam-ansible-poc object=/test.txt src=/tmp/test.txt mode=put
         
